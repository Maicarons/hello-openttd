# 安全策略 / Security Policy

## 受支持版本

项目处于早期开发阶段，暂无稳定版本发布。所有问题请直接针对 `main` 分支最新提交报告。

## 报告漏洞

**请勿使用公开 Issue 报告安全漏洞。**

请使用 GitHub 的 **"Report a vulnerability"**（仓库 Security 标签页 → Private vulnerability reporting）私密报告，或联系维护者。请在报告中包含：

1. 漏洞类型（如路径遍历、SSRF、下载劫持等）
2. 复现步骤与影响范围
3. 如果可能，附上 PoC 或相关日志

我们会在 **72 小时内**确认收到，**14 天内**给出初步评估，并协商披露时间线。

## 范围

**属于本项目的范围：**

- 启动器自身的路径遍历 / 任意文件读写
- 下载与镜像机制中的完整性问题（URL 校验绕过、校验和信任模型缺陷）
- 归档解压（Zip Slip、符号链接逃逸）
- 进程启动相关的注入/参数劫持
- 本地数据（设置、令牌）泄露

**不属于本项目范围：**

- OpenTTD 游戏本体的漏洞（请报告给 [OpenTTD 官方](https://github.com/OpenTTD/OpenTTD/security/advisories)）
- 用户自行添加的不可信镜像源/版本源造成的问题（信任模型中已明确标注为用户自担风险，但欢迎报告 UX 改进）
- 对已安装游戏的恶意篡改（属终端环境安全）

## 安全设计基线

设计与实现必须遵循 [安全设计文档](./docs/dev/security.md)，包括：路径规范化与根目录约束、HTTPS-only 与逐跳重定向校验、SHA-256 校验和固定与失败即停（fail-closed）、解压条目校验、依赖审计（Dependabot + 锁文件提交）。
